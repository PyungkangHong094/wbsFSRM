
  # Fishing App Schedule Management

  This is a code bundle for Fishing App Schedule Management. The original project is available at https://www.figma.com/design/d6oWL1lPP4OnwlpHXvCiDv/Fishing-App-Schedule-Management.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  